# Changelog

Documentation update history. Entries added automatically by `/ref-update`.

<!-- Add new entries at the top -->

## YYYY-MM-DD

- Updated `{doc_file}` — {N} files changed ({summary of what changed})
- Added `L3-domains/{domain}.md` — new domain
- Regenerated `{file}` — full refresh
