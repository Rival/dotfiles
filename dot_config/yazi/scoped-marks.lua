-- scoped-marks: настройки визуальных маркеров и поведения плагина.
-- Закладки хранятся не здесь: рантайм-источник —
-- ~/.local/state/yazi/scoped-marks/bookmarks.json (управление через B a / B d).
-- Изменения настроек рендера требуют перезапуска Yazi.

return {
	marker = {
		enabled = true,

		-- "name": Entity-child сразу после имени файла (левая часть строки).
		-- "linemode": Linemode-child в правой колонке рядом с size/mtime.
		slot = "name",

		-- nil выбирает default позиции: name = 4500, linemode = 1500.
		order = nil,

		-- Шаблоны маркеров; подставляется только {n} = число строгих
		-- потомков-закладок (для leaf это всегда 0).
		leaf = "★",      -- эта папка — закладка
		branch = "▾{n}", -- внутри есть закладки-потомки
		both = "★{n}",   -- закладка и есть потомки

		-- Добавляется перед непустым текстом маркера.
		separator = " ",

		-- nil берёт default (yellow); "reset" — явный терминальный default;
		-- "unset foreground" достижим только правкой самого default.
		fg = "yellow",
		bold = true,
	},

	-- Подхватывать внешние правки JSON при следующей смене каталога.
	-- Рантайм-изменения синхронизируются мгновенно через DDS и без этого.
	reload_on_cd = true,

	menu = {
		-- "pick" (default): плавающее меню у hover-строки, навигация стрелками.
		-- "which": легаси-селектор внизу экрана (спец §9).
		style = "pick",

		-- Только для pick: клавиши быстрого выбора у строк; false оставляет
		-- только стрелки/enter.
		quick_jump = true,

		-- Бек курсорной строки: nil = тема (th.pick.active — magenta+bold);
		-- цвет — hex или имя из палитры ratatui (имён orange/darkorange НЕТ,
		-- только hex/#rrggbb либо базовые имена — см. цветовой варнинг стапа).
		cursor_bg = "#ff8c00",

		-- Цвет текста курсорной строки — применяется ТОЛЬКО вместе с
		-- cursor_bg (чёрный на оранжевом — контрастно).
		cursor_fg = "black",

		-- Пул quick-клавиш под раскладку системы: упорядоченная строка
		-- символов (home row: s n t h k / q e a i c); зарезервированные
		-- строчные j/k/q/g автоматически пропускаются. nil = стандартный пул
		-- (asdfghjkl…). Строк сверх пула — только стрелки/enter.
		quick_pool = "snthkqeaic",

		-- Порог древовидности: закладок в блоке больше порога — группировка
		-- по общему префиксу (▸ группа (N), вход →/enter, выход ←); меньше или
		-- равно — плоский список. 0 = никогда не группировать.
		group_threshold = 10,

		-- Поведение ' при пустом scoped (в дереве cwd нет закладок):
		-- "all" — меню сразу со всеми закладками; "message" — пункт-сообщение
		-- «здесь нет, g покажет все».
		empty_scope = "all",

		-- Язык строк (уведомления, подтверждения, меню): "ru" | "en".
		lang = "ru",
	},
}
